name: Instagram Auto Poster (OLD - DISABLED)

on:
  workflow_dispatch:  # Manual only - disabled scheduled runs

jobs:
  post-to-instagram:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled. Use Instagram Auto Poster - Google Sheets instead."
